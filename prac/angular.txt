================ ANGULAR USER REGISTRATION / LOGIN / PROFILE APPLICATION ================

--------------------------------------------------
STEP 1: INSTALL ANGULAR CLI
--------------------------------------------------

Open terminal and run:

npm install -g @angular/cli

Check Angular version:

ng version

--------------------------------------------------
STEP 2: CREATE ANGULAR PROJECT
--------------------------------------------------

Run command:

ng new user-app

It will ask:

Would you like to add Angular routing?

Type:

No

Then ask:

Which stylesheet format would you like to use?

Choose:

CSS

--------------------------------------------------
STEP 3: OPEN PROJECT
--------------------------------------------------

Move inside project folder:

cd user-app

Open project in VS Code:

code .

--------------------------------------------------
STEP 4: RUN ANGULAR PROJECT
--------------------------------------------------

Run command:

ng serve

Open browser:

http://localhost:4200

--------------------------------------------------
STEP 5: CREATE COMPONENTS
--------------------------------------------------

Run commands one by one:

ng generate component register

ng generate component login

ng generate component profile

Shortcut Commands:

ng g c register

ng g c login

ng g c profile

--------------------------------------------------
FOLDER STRUCTURE / FILE ARCHITECTURE
--------------------------------------------------

user-app
│
├── src
│   │
│   ├── app
│   │   │
│   │   ├── register
│   │   │   ├── register.component.html
│   │   │   ├── register.component.ts
│   │   │   └── register.component.css
│   │   │
│   │   ├── login
│   │   │   ├── login.component.html
│   │   │   ├── login.component.ts
│   │   │   └── login.component.css
│   │   │
│   │   ├── profile
│   │   │   ├── profile.component.html
│   │   │   ├── profile.component.ts
│   │   │   └── profile.component.css
│   │   │
│   │   ├── app.component.html
│   │   ├── app.component.ts
│   │   └── app.module.ts
│   │
│   └── index.html
│
└── package.json

--------------------------------------------------
STEP 6: UPDATE app.module.ts
--------------------------------------------------

File Path:

src/app/app.module.ts

Code:

import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { FormsModule } from '@angular/forms';

import { AppComponent } from './app.component';
import { RegisterComponent } from './register/register.component';
import { LoginComponent } from './login/login.component';
import { ProfileComponent } from './profile/profile.component';

@NgModule({
  declarations: [
    AppComponent,
    RegisterComponent,
    LoginComponent,
    ProfileComponent
  ],

  imports: [
    BrowserModule,
    FormsModule
  ],

  providers: [],

  bootstrap: [AppComponent]
})

export class AppModule { }

--------------------------------------------------
STEP 7: app.component.html
--------------------------------------------------

File Path:

src/app/app.component.html

Code:

<h1>User Application</h1>

<app-register></app-register>

<hr>

<app-login></app-login>

<hr>

<app-profile></app-profile>

--------------------------------------------------
STEP 8: REGISTER COMPONENT
--------------------------------------------------

File:

register.component.html

Code:

<h2>Register User</h2>

<input type="text" [(ngModel)]="name" placeholder="Enter Name">

<br><br>

<input type="email" [(ngModel)]="email" placeholder="Enter Email">

<br><br>

<input type="password" [(ngModel)]="password" placeholder="Enter Password">

<br><br>

<button (click)="register()">Register</button>

--------------------------------------------------

File:

register.component.ts

Code:

import { Component } from '@angular/core';

@Component({
  selector: 'app-register',
  templateUrl: './register.component.html'
})

export class RegisterComponent {

  name = '';
  email = '';
  password = '';

  register() {

    let user = {
      name: this.name,
      email: this.email,
      password: this.password
    };

    localStorage.setItem("user", JSON.stringify(user));

    alert("User Registered");

  }

}

--------------------------------------------------
STEP 9: LOGIN COMPONENT
--------------------------------------------------

File:

login.component.html

Code:

<h2>Login User</h2>

<input type="email" [(ngModel)]="email" placeholder="Enter Email">

<br><br>

<input type="password" [(ngModel)]="password" placeholder="Enter Password">

<br><br>

<button (click)="login()">Login</button>

--------------------------------------------------

File:

login.component.ts

Code:

import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  templateUrl: './login.component.html'
})

export class LoginComponent {

  email = '';
  password = '';

  login() {

    let user = JSON.parse(localStorage.getItem("user") || '{}');

    if (
      this.email == user.email &&
      this.password == user.password
    ) {

      alert("Login Successful");

    } else {

      alert("Invalid Email or Password");

    }

  }

}

--------------------------------------------------
STEP 10: PROFILE COMPONENT
--------------------------------------------------

File:

profile.component.html

Code:

<h2>User Profile</h2>

<p>Name: {{user.name}}</p>

<p>Email: {{user.email}}</p>

--------------------------------------------------

File:

profile.component.ts

Code:

import { Component } from '@angular/core';

@Component({
  selector: 'app-profile',
  templateUrl: './profile.component.html'
})

export class ProfileComponent {

  user = JSON.parse(localStorage.getItem("user") || '{}');

}

--------------------------------------------------
FINAL RUN COMMAND
--------------------------------------------------

ng serve

Open Browser:

http://localhost:4200

--------------------------------------------------
WORKING FLOW
--------------------------------------------------

1. User registers
2. Data stored in localStorage
3. User logs in
4. Profile component displays user data

--------------------------------------------------
IMPORTANT VIVA QUESTIONS
--------------------------------------------------

Q1. What is Angular?

Answer:
Angular is frontend framework used to build web applications.

--------------------------------------------------

Q2. What is Angular CLI?

Answer:
Tool used to create and manage Angular projects.

--------------------------------------------------

Q3. What is Component?

Answer:
Component controls one part of webpage.

--------------------------------------------------

Q4. What is ngModel?

Answer:
Used for two-way data binding.

--------------------------------------------------

Q5. Why FormsModule is imported?

Answer:
To use ngModel.

--------------------------------------------------

Q6. What is localStorage?

Answer:
Browser storage used to store data.

--------------------------------------------------

Q7. What is interpolation?

Answer:
{{ }} is used to display dynamic data.

--------------------------------------------------

Q8. What does (click) do?

Answer:
Handles button click event.

--------------------------------------------------

Q9. What is JSON.stringify()?

Answer:
Converts object into string.

--------------------------------------------------

Q10. What is JSON.parse()?

Answer:
Converts string into object.