1) open GitHub on browser
2) create repository
3) create folder and put any file in the folder that you want push
   open this folder on vs code 
4) in vs code open that folder 
   open terminal and excute following commonds


git init

git config --global user.name "github-username"

git config --global user.email "git emailid"

git add .

git commit -m "first commit"

git branch -M main

git remote add origin https://github.com/username/repositoryname.git

git push -u origin main




------------------------------------------------------------------
example
------------------------------------------------------------------
1) open GitHub on browser
2) create repository
3) create folder and put any file in the folder that you want push
   open this folder on vs code 
4) in vs code open that folder 
   open terminal and excute following commonds


git init

git config --global user.name "Sagar3060"

git config --global user.email "sagarpathade810@gmail.com"

git add .

git commit -m "first commit"

git branch -M main

git remote add origin https://github.com/Sagar3060/demo.git

git push -u origin main
----------------------------------------------------------------
